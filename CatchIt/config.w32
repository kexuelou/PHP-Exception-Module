// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("catchit", "for catchit support", "no");

// Otherwise, use 

ARG_ENABLE("catchit", "enable catch support", "no");

if (PHP_CATCHIT != "no") {
	EXTENSION("catchit", "catchit.c");
}

