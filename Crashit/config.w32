// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("crashit", "for crashit support", "no");

// Otherwise, use ARG_ENABLE
ARG_ENABLE("crashit", "enable crashit support", "no");

if (PHP_CRASHIT != "no") {
	EXTENSION("crashit", "crashit.c");
}

